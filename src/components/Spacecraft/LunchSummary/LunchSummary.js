import React from 'react';
import Aux from './../../../hoc/ReactAux'

const lunchSummary = (props) => {
    const partsSummary = props.parts
    return (
        <Aux>
            <h3>Your lanch details:</h3>
        </Aux>
    )
};

export default lunchSummary;
